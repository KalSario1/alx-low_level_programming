#include "main.h"

/**
 * main - function
 * @argc: argument count
 * @argv: argument vector
 * Return: Int or 0
*/


int main(int argc, char *argv[])
{
int i;
for (i = 0; i < argc; i++)
{
printf("%d\n", argc[argv]);
}
return (0);
}
