#include "main.h"

/**
 * main - function
 * @argc: argument count
 * @argv: argument vector
 * Return: Int or 0
*/


int main(int argc, char *argv[])
{
if (argc > 0)
{
printf("%s\n", argv[0]);
}
return (0);
}
