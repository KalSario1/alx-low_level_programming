#include "main.h"

/**
 * _abs - check if words that are undercase
 * @n: the charcter checker
 * Return: 1 if c is lowercase, 0 if otherwise
*/

int _abs(int n)
{
if (n >= 0)
{
    return (n);
}
    return (-n);
}
