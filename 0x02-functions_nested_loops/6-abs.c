#include "main.h"

/**
 * _isalpha - check if words that are undercase
 * @c: the charcter checker
 * Return: 1 if c is lowercase, 0 if otherwise
*/

int _abs(int n)
{
if (n >= 0 && n <0)
{
    return (abs(n));
}
}
