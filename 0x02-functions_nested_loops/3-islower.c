#include "main.h"

/**
 * _islower - check if words that are undercase
 * @c: the charcter checker
 * Return: 1 if c is lowercase, 0 if otherwise
*/

int _islower(int c)
{
return (c >= 'a' && c <= 'z');

}
